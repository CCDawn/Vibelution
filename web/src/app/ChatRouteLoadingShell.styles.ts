const styles = {
  shell: "min-w-0 w-full",
  srOnly: "sr-only",
  paneFill: "min-h-0",
  indexSkeleton: "min-h-0 flex-1 overflow-hidden p-1",
  workspaceSkeleton: "min-h-0 h-full",
  statusRailSkeleton: "p-1",
} as const;

export default styles;
