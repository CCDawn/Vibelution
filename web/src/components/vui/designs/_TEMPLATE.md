# Component: `{{Name}}`

> 复制本模板到对应族文件末尾，或新建 `designs/<layer>/<name>.md`（仅当该组件无法归入现有族文件时）。
> **设计选型三件套（必填）**：功能 · 适用范围 · 使用方式 — 先写这三节，再补实现细节。

## {{Name}}

### 功能
（一句话：它解决什么 UI 问题、用户得到什么）

### 适用范围
- **适用**：…
- **不适用**（改用 `…`）：…

| 场景 | 选择 |
| --- | --- |
| … | 用本组件 |
| … | 改用 `…` |

### 使用方式
```tsx
// 最小可运行示例（真实 import 路径以 index 为准）
import { {{Name}} } from "@/components/vui";

<{{Name}} /* 关键 props */ />
```

| Prop / 槽位 | 说明 | 设计注意 |
| --- | --- | --- |
| … | … | … |

### 非职责
- …

### 视觉与状态
- 默认 / hover / focus / disabled / loading / empty / error（按需）

### 实现落点
- 源码：`…`
- Renderer（如有）：`…`

### 反冗余
- 与 `…` 的边界：…
- 禁止再新建：…
