const styles = {
  trigger:
    "vui-routes-launcherbranchstatushelp trigger inline-flex cursor-help rounded-sm focus-visible:outline-none focus-visible:shadow-[var(--vui-shadow-focus)]",
} as const;

export default styles;
