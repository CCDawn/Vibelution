/**
 * Teams route entry (stable import path for router + layout contracts).
 * Thin shell: teams/TeamsRouteWorkbench.tsx → useTeamsWorkbenchModel.
 */
export { TeamsRoute, linkedRoomRefetchInterval } from "./teams/TeamsRouteWorkbench";
