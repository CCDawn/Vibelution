# Superpowers 历史规格与计划（已归档）

> **状态：非现行权威 · 路径：`docs/archive/superpowers/`**
> 原位于 `docs/superpowers/`；2026-08 整树迁入 archive。
> 现行规则：[`docs/standards/README.md`](../../standards/README.md) · 根 [`AGENTS.md`](../../../AGENTS.md)

## 何时可以读

- 追溯某次功能的设计动机
- 对照「当时计划 vs 现在实现」
- 考古已完成迁移（如 HeroUI → VUI、LLM protocol 统一）

## 何时不要读

- 作为配置、协议、UI 红线的**当前**依据
- 覆盖 `docs/ops/config/`、`core/llm/PROTOCOL.md` 或 `docs/agents/` 现行地图

## 子目录

| 路径 | 内容 |
| --- | --- |
| `plans/` | 带日期的实现计划（多数已 implemented/superseded） |
| `specs/` | 设计规格快照 |
| `evidence/` | 局部验收证据（如 checklist） |

单文件内的 Status 字段若与现行代码冲突，**以代码与 standards 为准**。

## 路径提示

文内自引用可能仍写 `docs/superpowers/...`；在磁盘上等价于 `docs/archive/superpowers/...`。
