# Self-Evolving Agent 依赖包

# 核心依赖
requests>=2.28.0
tiktoken>=0.7.0            # Token 计数（用于日志实时显示输入 token 数）
# tiktoken 的 regex 依赖在 Python 3.12/Windows 上必须命中 wheel，避免源码元数据构建卡住。
regex==2026.5.9
psutil>=5.9.0
rich>=13.7.0              # CLI 富文本渲染引擎 (Claude Code 级 TUI)

# LangChain 依赖
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.2
litellm>=1.51.0
openai[realtime]>=2.38.0
socksio>=1.0.0             # httpx SOCKS proxy support for LiteLLM when ALL_PROXY is set
mcp==2.0.0                 # Official MCP Python SDK v2; legacy + modern protocol eras

# Research workflow runtime (ADR 0006) — direct deps, not transitive-only
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
apsw==3.53.4.0            # Canonical conversation WAL runtime; bundles fixed SQLite per project


# 配置管理 (Pydantic v2)
pydantic>=2.0.0
fastapi>=0.111.0
uvicorn>=0.30.0
tzdata>=2025.2            # Windows/精简 Python 运行时的 IANA 时区数据库
pypdf>=5.0.0              # 本地 PDF 文本抽取（Challenge Cup source-extraction 页码锚点）
pywinpty>=3.0.5; platform_system == "Windows"  # Windows ConPTY backend for embedded CLI Agent terminals
ddgs>=9.0.0               # No-key public metasearch fallback for research search tools
trafilatura>=2.0.0        # Web article extraction for web_fetch_tool
pyrate-limiter>=4.0.0     # global per-provider rate limiting for source collection

# 可选依赖（根据需要安装）
# beautifulsoup4>=4.12.0  # 用于更高级的 HTML 解析
# lxml>=4.9.0            # XML/HTML 解析器（beautifulsoup4 的后端）

# 开发/测试依赖
pytest>=7.0.0            # 单元测试框架
pytest-asyncio>=0.21.0   # 异步测试支持
pytest-timeout>=2.0.0    # 单测试超时保护（Windows 下用 thread 方法防挂死）
pytest-xdist>=3.6.1      # pytest 进程级并行执行（可选启用）
coverage>=7.0.0          # 测试覆盖率门禁
jedi>=0.19.0             # Python 符号分析（定义 / 引用 / 悬浮）
ruff>=0.6.0              # Python lint 诊断
# black>=23.0.0          # 代码格式化
# mypy>=1.0.0            # 类型检查
