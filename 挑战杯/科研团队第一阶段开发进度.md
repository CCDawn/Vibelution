# 挑战杯科研团队第一阶段开发进度

更新时间：2026-06-18 17:36:40 +08:00

适用范围：Vibelution `ai科学研究团队` 的挑战杯科研流程，重点是第一阶段 `知识搜集`。本文用于下一轮开发规划，不是用户操作手册。

## 1. 当前结论

当前第一阶段已经从旧的多节点流程收敛为四个用户可理解步骤：

1. 搜索资料
2. 资料提炼
3. 资料审查
4. 资料入库

其中：

- `搜索资料` 已经具备可运行链路：创建批次、生成搜索计划、分配查询、执行 metadata-only 搜索、写入 DataRecord、导入 source_manifest 候选。
- `资料审查` 已经具备 Agent 批处理链路：资料质量评估 Agent 可批量审查候选资料，也支持 force 重新审查。
- `资料入库` 已经具备一键链路：复用资料审查、候选关系图、入库包、Team Knowledge source/proposal/review 门禁，最终可写入正式 KnowledgeItem。
- `资料提炼` 当前主要是“候选资料可视化展示页”，还不是一个完整的一键 Agent 批处理阶段。这是下一轮最高优先级缺口。

项目 HTML 与代码目前已同步，`挑战杯/research_team_flow_design.html` 已包含 `M6.55 知识搜集四步闭环入库`。

## 2. 当前代码落点

### 2.1 后端核心文件

- `core/web/services/team_workflow_orchestration_service.py`
  - 搜索资料批次创建：`start_source_collection_run`
  - 搜索执行：`execute_source_collection_search`
  - DataRecord 导入候选：`import_data_record_as_source_candidate`
  - 资料审查批量执行：`assess_source_quality_batch`
  - 候选关系图生成：`build_candidate_graph`
  - 入库前审：`run_knowledge_ingestion_precheck`
  - 第一阶段一键入库：`run_knowledge_collection_ingestion`
  - 入库状态聚合：`get_knowledge_ingestion_status`

- `core/web/routes/team_workflows.py`
  - `POST /api/teams/{team_id}/workflow-orchestration/source-collection-runs`
  - `POST /api/teams/{team_id}/workflow-orchestration/source-collection-runs/{run_id}/search/execute`
  - `POST /api/teams/{team_id}/workflow-orchestration/data-processing/runs/{run_id}/records/{record_id}/source-candidate`
  - `POST /api/teams/{team_id}/workflow-orchestration/source-quality/assess-batch`
  - `POST /api/teams/{team_id}/workflow-orchestration/candidate-graph`
  - `POST /api/teams/{team_id}/workflow-orchestration/knowledge-ingestion/precheck`
  - `POST /api/teams/{team_id}/workflow-orchestration/knowledge-collection/ingest`

- `core/web/services/data_processing_service.py`
  - 通用 DataProcessingRun、CollectionAssignment、CollectionOutput、DataRecord 能力已复用。
  - 当前不建议为了挑战杯继续改这个通用服务，优先在 workflow 层补桥接能力。

### 2.2 前端核心文件

- `web/src/routes/TeamsRoute.tsx`
  - ai科学研究团队入口。
  - 三阶段科研控制台：知识搜集、实验、迭代。
  - 知识搜集独立页。
  - 四张步骤卡：搜索资料、资料提炼、资料审查、资料入库。
  - 每张卡显示状态、关键数量、主按钮和详情入口。
  - 当前阶段详情右侧显示步骤 Agent 配置。
  - 当前阶段结果页与 Agent 过程页拆分。
  - 来源类型过滤：全部、PDF、论文网页/DOI、数据集、本地文件、缺少来源。
  - 资料卡可点击，右侧显示 DOI、网页、本地文件、批次目录、搜索步骤、搜集记录和候选镜像入口。

### 2.3 挑战杯 HTML

- `挑战杯/build_research_flow_site.mjs`
  - 挑战杯规划 HTML 生成源。

- `挑战杯/research_team_flow_design.html`
  - 当前规划索引页。
  - 已包含 M6.55 四步闭环入库说明。

- `挑战杯/research_flow_pages/**`
  - 生成的子页面。

## 3. 已完成能力

### 3.1 团队入口与页面结构

已完成：

- 团队页面顶部固定团队选择，不再用左侧团队创建区承载挑战杯流程。
- ai科学研究团队进入后展示科研控制台，而不是先展示组织画布。
- 组织画布保留为附属结构视图，不再占据主流程空间。
- 知识搜集、实验、迭代被作为三个一级阶段。
- 知识搜集点击后进入独立阶段页。
- 知识搜集阶段页只展示四步主流程，不再把候选关系图和入库包作为独立用户步骤。

剩余问题：

- 前端内部仍保留一些旧 id 和旧视图命名，例如 `collection/screening/candidate/graph/memory`。
- 用户看到的是四步，但代码里仍有五段旧语义残留，后续维护容易混乱。

### 3.2 搜索资料

已完成：

- 用户可启动知识搜集批次。
- 系统生成 `DataSearchPlan/querySeeds`。
- 系统创建通用 `DataProcessingRun`。
- 系统创建 `CollectionAssignment`，把查询分配给功能 Agent。
- 支持无 key 多源 metadata 搜索，优先使用 OpenAlex、arXiv、SearXNG、DDGS 等公开来源，并在写入 DataRecord 前过滤低相关、域名不匹配或明显噪声结果。
- 搜索结果写入 DataRecord。
- DataRecord 会自动导入 source_manifest 候选。
- 支持“搜索下一批”。
- 支持 activeWorkRun 显示后台搜索状态。
- 搜索过程事件进入对话式过程流。
- 搜索结果和搜索过程分离展示。
- 搜索结果可按来源类型过滤。
- 结果卡显示 DOI、网页、本地路径或缺少来源。
- 机器 API/公开索引证据被放入搜索证据区域，而不是直接把索引 JSON 当成用户正文。

边界：

- 当前搜索是 metadata-only，不下载全文 PDF。
- 不写正式 Team Knowledge。
- 不写 RAG。
- 不写正式知识图谱。

剩余问题：

- 重复搜索会产生重复 DataRecord；虽然候选导入可以幂等，但原始资料记录数量会膨胀。
- DataRecord 与 source_manifest 的数量关系仍需要更清晰的统一投影，否则用户容易看到“已搜索 40，但页面只显示 8”之类的错觉。
- 搜索过程还没有完全复用对话 UI 的成熟消息组件，当前更像服务事件时间线。

### 3.3 资料提炼

当前状态：

- 已有 source_manifest 候选库。
- 已有 DataRecord -> source_manifest 的导入桥。
- 前端有“资料提炼结果”页面，可展示本轮候选资料。
- 资料提炼页面能避免把原始 DataRecord 与候选资料混在一起。

真实缺口：

- 当前“资料提炼”不是一个明确的一键 Agent 动作。
- 点击资料提炼更多是查看候选库，而不是让 Agent 执行“把原始资料转成可审查候选资料”的阶段。
- 搜索执行时会自动导入候选，所以用户容易误以为“提炼”没有发生，或者不知道是哪个 Agent 做的。
- 如果存在未导入候选的 DataRecord，目前没有一个专门的“资料提炼 Agent 批处理”按钮来补导入。

建议下一步：

- 新增 workflow 层 API：`POST /api/teams/{team_id}/workflow-orchestration/knowledge-collection/extract`
- 输入：teamId、runId、agentId、maxRecords、force。
- 行为：
  - 读取本轮 DataRecord。
  - 找出还没有 source_manifest 候选镜像的记录。
  - 调用既有 `import_data_record_as_source_candidate`。
  - 写入 Agent 过程事件。
  - 返回 imported/skipped/failed 统计。
- 前端：
  - 资料提炼卡主按钮改成 `Agent 提炼资料`。
  - 已无待提炼记录时显示 `查看提炼结果` 或 `重新提炼`。
  - 右侧当前步骤 Agent 配置显示资料提炼 Agent 的名称、模型、状态和配置入口。

### 3.4 资料审查

已完成：

- 资料质量评估 Agent 可批量执行。
- API：`assess_source_quality_batch`
- Route：`POST /api/teams/{team_id}/workflow-orchestration/source-quality/assess-batch`
- 写入 `sourceQualityAssessment/sourceQualityAssessments`。
- 候选状态可以进入：
  - 通过
  - 需修订
  - 拒绝
  - 待审
- 已审完时，前端按钮可变为 Agent 重新审查，并通过 `force=true` 复审。
- 资料审查页有完整候选列表、结果过滤和右侧 Agent 配置。

剩余问题：

- 评分和通过规则目前偏启发式，适合 MVP，但科研质量判断还需要更结构化的标准。
- “已筛选/已审/通过/候选”几个统计口径仍需统一，否则顶部卡片和底部列表可能不一致。
- 需增加“审查依据”展示，让用户看到为什么通过或退回。

### 3.5 资料入库

已完成：

- 一键入库 API：`run_knowledge_collection_ingestion`
- Route：`POST /api/teams/{team_id}/workflow-orchestration/knowledge-collection/ingest`
- 内部复用现有治理门禁，不直接绕过正式知识库边界。
- 当前一键链路包含：
  - 资料审查批处理
  - 生成候选关系图
  - 生成资料入库包
  - 提交 Team Knowledge 来源收件箱
  - 通过来源 review
  - 生成知识库 proposal
  - 通过知识 review
  - 创建正式 KnowledgeItem
- 如果没有知识库，可自动创建 `挑战杯科研知识库`。

边界：

- 正式 KnowledgeItem 可以创建。
- 正式 RAG 写入仍未默认打开。
- 正式 official graph 同步仍遵守现有审核边界。

剩余问题：

- 用户口中的“入库”可能包含团队共享记忆、知识库、RAG、知识图谱四层。当前需要在 UI 上明确显示四层状态：
  - 团队知识库 KnowledgeItem
  - 团队共享记忆
  - RAG
  - 正式知识图谱
- 当前“资料入库”内部仍包含候选图和入库包等复杂产物，UI 已合并，但代码语义仍偏旧。

## 4. 测试与验证进展

上一轮已验证过：

- 后端测试：
  - `tests/test_team_workflow_orchestration_service.py`
  - `tests/test_team_workflow_routes.py`
  - 结果：89 passed
- 前端布局测试：
  - `npm --prefix web run test -- TeamsRoute.layout.test.ts`
  - 结果：13 passed
- 前端构建：
  - `npm --prefix web run build`
  - 结果：BUILD_OK
- HTML 生成：
  - `node 挑战杯/build_research_flow_site.mjs`
  - 结果：Generated 13 research flow pages
- `git diff --check`
  - 通过

本次文档整理未重新运行完整测试，因为没有改业务代码。

## 5. 当前主要风险

### 5.1 四步用户模型与旧代码模型不完全一致

用户看到的是：

1. 搜索资料
2. 资料提炼
3. 资料审查
4. 资料入库

代码里仍残留：

- collection
- candidate
- screening
- graph
- memory

风险：

- 后续开发容易把“候选图”和“入库包”重新做成用户步骤。
- 统计口径分散，导致顶部卡片和底部列表不一致。
- 资料提炼没有独立 API 时，用户会觉得第二步是空的或只是查看页。

### 5.2 统计口径未统一

目前常见统计来源：

- DataProcessingRun records
- CollectionAssignment queries
- CandidateStore source_manifest
- sourceQualityStatus
- candidateGraph summary
- knowledgeIngestionStatus
- 前端当前筛选/分页列表

风险：

- 顶部显示 `通过 31`，底部当前页只显示 6 条。
- 用户无法区分“总数、当前页、当前过滤条件、本轮候选、正式入库”。

建议：

- 增加统一投影：`KnowledgeCollectionStageProjection`
- 每个阶段只从一个 projection 读：
  - total
  - filtered
  - page
  - pending
  - completed
  - failed
  - nextAction

### 5.3 Agent 过程仍不够像对话链路

已经有过程页，但它更像 workflow event 时间线。

用户需要的是：

- 哪个 Agent 在什么时候做了什么。
- 输入是什么。
- 输出是什么。
- 写到哪里。
- 下一步交给谁。
- 失败时为什么失败。

建议：

- 复用对话消息组件或建立兼容 `ConversationTimelineMessage` 的投影。
- 搜索结果和 Agent 过程继续保持两个子页：
  - 结果：只看资料。
  - 过程：只看 Agent 执行流水。

### 5.4 搜索结果来源仍需加强可溯源

已做 DOI/网页/本地文件区分，但还需要稳定规则：

- DOI：显示 DOI 和 `https://doi.org/...`
- 网页：显示页面标题和来源 URL
- 本地 PDF：显示本地路径、sha256、页码
- 数据集：显示 dataset id 或下载页
- API 证据：默认折叠为“搜索证据”
- 无来源：显式标红或标黄，不能混入正常资料

### 5.5 搜索噪声与去重不足

目前 repeated search 会造成：

- DataRecord 变多。
- 候选导入可能因幂等不重复，但用户仍看到原始资料列表有重复。
- 候选图可能出现重复节点或低价值关系。

建议：

- 增加 DOI/sourceRef/title 归一化去重。
- 搜索资料结果默认展示“去重后的资料”，高级模式再看原始 DataRecord。
- 候选图只使用通过审查且去重后的候选。

## 6. 下一步建议开发顺序

### M6.56：统一四步阶段投影

目标：

- 后端提供或前端集中计算一个统一 projection。
- 解决顶部卡片、底部列表、右侧详情数量不一致。

建议输出：

- `stageId`
- `label`
- `status`
- `inputCount`
- `outputCount`
- `totalCount`
- `filteredCount`
- `pageRange`
- `pendingCount`
- `completedCount`
- `nextAction`
- `agentBindings`
- `artifacts`

验收：

- 顶部卡片和当前子页使用同一组数字。
- 切换过滤和分页时，明确显示当前页范围，不影响总数。

### M6.57：资料提炼 Agent 一键批处理

目标：

- 让“资料提炼”变成真实可执行阶段，而不是只查看候选库。

建议实现：

- 新增 service：`extract_source_collection_candidates`
- 新增 route：`POST /knowledge-collection/extract`
- 复用：
  - `data_processing_service.list_records`
  - `import_data_record_as_source_candidate`
- 不改通用 data_processing_service。

验收：

- 搜索资料完成后，资料提炼按钮可点击。
- 点击后导入未导入的 DataRecord。
- 已导入的记录不会重复创建候选。
- UI 显示提炼 Agent、模型、输入记录数、输出候选数、跳过数、失败数。

### M6.58：Agent 过程页对话化

目标：

- 每个阶段都能展示“像对话一样”的执行过程。

建议：

- 建立 `StageConversationEvent` 投影。
- 字段：
  - messageId
  - stageId
  - agentName
  - agentRole
  - modelName
  - action
  - inputSummary
  - outputSummary
  - artifactRefs
  - status
  - createdAt
- 过程页按消息流展示，不再直接暴露 raw workflow event。

验收：

- 用户可以看到 Agent 依次搜索、写记录、提炼、审查、入库。
- 每条过程都有打开证据或存放位置的按钮。

### M6.59：资料卡溯源统一

目标：

- 每个资料卡都能清楚回答“这是什么、来自哪里、能不能打开”。

建议：

- 建立统一 `SourceCardViewModel`。
- 来源类型：
  - DOI
  - paper_url
  - pdf_file
  - dataset
  - local_file
  - api_evidence
  - missing_source

验收：

- 点击卡片不会默认打开公开索引 JSON。
- 主按钮优先打开 DOI、论文网页或本地文件。
- API 原始搜索结果只在“搜索证据”里看。

### M6.60：资料入库状态四层化

目标：

- 明确第一阶段最终到底入了哪里。

建议 UI 显示四层状态：

1. 团队知识库 KnowledgeItem
2. 团队共享记忆
3. RAG
4. 正式知识图谱

验收：

- 资料入库完成后，用户知道哪些已完成，哪些仍在治理边界外。
- 不再把 CandidateStore、KnowledgeItem、RAG、official graph 混称为“入库”。

## 7. 推荐下一轮最小任务

建议下一轮不要继续大范围重做 UI，而是先做一个清晰的垂直切片：

任务名：补齐资料提炼 Agent 一键阶段

范围：

- `core/web/services/team_workflow_orchestration_service.py`
- `core/web/routes/team_workflows.py`
- `web/src/routes/TeamsRoute.tsx`
- `tests/test_team_workflow_orchestration_service.py`
- `tests/test_team_workflow_routes.py`
- `挑战杯/build_research_flow_site.mjs`
- 生成的挑战杯 HTML

不做：

- 不改通用 `data_processing_service.py`
- 不接真实全文下载
- 不写 RAG
- 不写正式图谱
- 不重做实验/迭代阶段

验收标准：

- 资料提炼卡有明确 Agent 主按钮。
- 点击后可批量导入本轮未提炼 DataRecord。
- 过程页能看到资料提炼 Agent 做了什么。
- 结果页能看到提炼出来的候选资料。
- 重复点击不会重复创建候选。
- 后端测试和前端构建通过。
- 挑战杯 HTML 同步更新。

## 8. 当前工作区状态

主仓：

- 路径：`C:\Users\17533\Desktop\Vibelution`
- 分支：`main`
- 状态：领先远端，多轮挑战杯改动已合并到本地主线。

当前临时 worktree：

- 路径：`C:\Users\17533\Desktop\Vibelution-worktrees\challenge-extraction-step`
- 分支：`codex/challenge-extraction-step`
- 目的：原本准备实现资料提炼 Agent 一键阶段。
- 当前状态：未改业务代码，本文件是本轮文档产物。

已登记 claim：

- `claim-20260618T091130-codex-challenge-extraction-step-4eefe053`
- lane：`web-workbench-surface`
- scope：挑战杯 workflow service、route、TeamsRoute、相关测试、挑战杯 HTML。

## 9. 判断

第一阶段已经不是“没有流程”的状态，而是已经有了可跑通的主干：

搜索资料 -> 候选资料 -> Agent 审查 -> 一键入库到 Team Knowledge

但要达到用户期望的“清楚、美观、每一步可一键、每一步知道 Agent 做了什么”，还差三个关键工程动作：

1. 统一阶段投影，根治数量和状态不一致。
2. 补齐资料提炼 Agent 一键阶段，根治第二步像查看页的问题。
3. 对话化 Agent 过程页，根治黑盒感。

优先顺序建议为：

1. M6.57 资料提炼 Agent 一键阶段
2. M6.56 统一阶段投影
3. M6.58 Agent 过程页对话化

原因：先让四步每一步都真正可执行，再统一数字和页面表达，最后提升过程可读性。
